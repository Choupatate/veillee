# Where story folders live. Back this directory up — it is all your data.
STORYBOOK_STORIES_DIR=./stories

# Shared password for the one login this app has. Required in production;
# if unset, dev mode accepts the password "dev".
STORYBOOK_PASSWORD=changeme

# Flask session-signing key. Required whenever STORYBOOK_PASSWORD is set (the
# app refuses to start otherwise). In dev mode (no password set) a random one
# is generated at startup, which invalidates sessions on restart.
STORYBOOK_SECRET_KEY=change-this-to-a-long-random-string

# Set to 1 when serving over HTTPS to mark the session cookie Secure (it is
# never sent over plain HTTP) and to send a Strict-Transport-Security
# header. Leave unset/0 for local/LAN HTTP use.
STORYBOOK_COOKIE_SECURE=0

# How many reverse proxies sit in front of the app (0 = none, the default).
# Set to 1 when running behind nginx/Caddy/Traefik/a NAS reverse proxy so
# the app sees each visitor's real IP (via X-Forwarded-For) instead of the
# proxy's — the login lockout counts failed attempts per IP, and without
# this every visitor would share the proxy's address. Never set it higher
# than the number of proxies you actually control: trusting forwarded
# headers nobody sets lets clients fake their IP.
STORYBOOK_TRUSTED_PROXIES=0

# Optional: the book's own language ("en" or "fr"), used for a visitor who
# hasn't picked one from the flags in the nav and whose browser expresses no
# preference. Each person's own choice from the picker always wins, and is
# remembered on their device for a year. Defaults to English.
# STORYBOOK_LANGUAGE=fr

# Optional: let several family members write in the same shared timeline,
# each with their own color. Comma-separated Name:#hexcolor pairs. Leave
# unset to disable the feature entirely (no author picker, no bylines, no
# legend). Renaming an author here does not rewrite already-saved stories.
# STORYBOOK_AUTHORS="Papa:#d9a441,Maman:#7ba7d9"

# Optional: the child's birth date. When set, every story and timeline entry
# shows the child's age at that memory (e.g. "2 years old"). Leave unset to
# disable the feature entirely.
# STORYBOOK_BIRTHDATE="2023-06-18"

# Optional: the app's display name — used in the nav, page titles, the
# home-screen install manifest, and the book cover (/book). Defaults to
# "Storybook".
# STORYBOOK_TITLE="Le livre de Milo"

# Optional: the slug of the person page the family tree's kinship labels
# ("your grandmother", "your uncle") are computed relative to. Create a
# person for your child first, then point this at their slug. Leave unset
# (or point it at a slug that doesn't exist) to disable computed labels —
# the tree itself and the Family sections on person pages still work, just
# without the "your ___" wording. When the book is inherited, re-point this
# one line and every label re-anchors to the next generation.
# STORYBOOK_CHILD="milo"

# Optional: per-person username/password accounts with an admin role,
# instead of the one shared password above (FEATURES.md F19). Off by
# default — leave unset and nothing about this app changes. When on,
# STORYBOOK_PASSWORD never logs anyone in directly; it's only the invite
# code required to submit a request at /request-account. The first request
# ever submitted auto-approves as admin; every one after that waits for an
# admin to review it.
# STORYBOOK_ACCOUNTS=1

# Optional: let someone request an account without knowing the invite code
# above, waiting for an admin to approve them instead (FEATURES.md F39).
# Off by default — with it unset, the code is required as before. Needs
# STORYBOOK_ACCOUNTS=1 to mean anything. A wrong code still fails either
# way, and a codeless request can never become the auto-approved first
# admin: bootstrapping the book always requires the code. Independent of
# admin-issued invitations (Accounts -> + Invite), which need no env var.
# STORYBOOK_OPEN_REQUESTS=1
